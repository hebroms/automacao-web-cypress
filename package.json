{
  "name": "automacaocypress",
  "version": "1.0.0",
  "description": "Automação de testes com Cypress",
  "main": "index.js",
  "scripts": {
    "cy:run": "cypress run",
    "allure:generate": "allure generate allure-results --clean -o allure-report",
    "allure:open": "allure open allure-report",
    "generate:html:report": "ts-node generate-report.ts",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Hebrom Chaves de Souza",
  "license": "ISC",
  "devDependencies": {
    "@badeball/cypress-cucumber-preprocessor": "^22.0.1",
    "@bahmutov/cypress-esbuild-preprocessor": "^2.2.4",
    "@faker-js/faker": "^9.6.0",
    "@shelex/cypress-allure-plugin": "github:shelex/cypress-allure-plugin",
    "allure-commandline": "^2.33.0",
    "cypress": "^14.2.0",
    "cypress-mochawesome-reporter": "^3.8.2",
    "cypress-multi-reporters": "^2.0.5",
    "esbuild": "^0.25.1",
    "multiple-cucumber-html-reporter": "^3.9.2"
  }
}
